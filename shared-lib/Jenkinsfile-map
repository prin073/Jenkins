@Library("shared-library") _

pipeline {
        agent any
        
        stages {
            stage ("HelloWorldMap") {
                steps {
                
                    helloWorldMap(name: "Prince", weekOfDay: "Thursday")
                    helloWorldMap(weekOfDay: "Thursday", name: "Prince") // order doesn't matter
                }
            
            }
        
        }
}