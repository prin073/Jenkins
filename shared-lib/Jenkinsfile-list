@Library("shared-library") _

pipeline {
        agent any
        
        stages {
            stage ("HelloWorldList") {
                steps {
                  helloWorldList(["Prince", "Ankit", 1, 2])
                }
            
            }
        
        }
}