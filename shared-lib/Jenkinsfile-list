@Library("shared-library") _

pipeline {
        agent any
        
        stages {
            stage ("HelloWorldList") {
                steps {
                  helloWorldListForEach(["Prince", "Ankit", 1, 2])
                }
            
            }
        
        }
}